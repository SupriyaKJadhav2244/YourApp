import { MobileFilterPipe } from './mobile-filter.pipe';

describe('MobileFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MobileFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
